__pycache__/
*.pyc
.venv/
docker-compose.override.local.yaml

# Live instance data - host-specific config/stacks from running the dev
# sandbox, not seed data.
config/

# Scratch todo list, not for the public repo.
TODO

# Agent-facing project instructions - kept local/private, not shipped
# in the public repo.
CLAUDE.md

# Personal deploy helper - hardcodes this host's /docker/litethaus*
# paths, not portable, not shipped in the public repo.
scripts/deploy.sh

# Secrets - nothing like this exists in the repo today, but ignored
# proactively so a future mistake doesn't get committed.
.env
.env.*
*.pem
*.key
id_rsa*
id_ed25519*
*credentials*

# Playwright MCP session output (screenshots, snapshots, console logs)
.playwright-mcp/
