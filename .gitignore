__pycache__/
*.pyc
.venv/
docker-compose.override.local.yaml

# Live instance config - holds host-specific settings and the admin
# password hash. ConfigService auto-generates this with defaults on
# first run (see backend/config_service.py DEFAULT_CONFIG), so it
# never needs to be tracked or shipped as seed data.
data/config.yaml

# Agent-facing project instructions - kept local/private, not shipped
# in the public repo.
CLAUDE.md
